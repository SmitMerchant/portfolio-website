# All variables below are SERVER ONLY. Never prefix secrets with VITE_.
# Keep AI off until the leaked key is revoked and fresh credentials are configured.
CHAT_ENABLED=false
DEEPSEEK_API_KEY=
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
# Generate at least 32 random characters; used to hash IPs in usage counters.
CHAT_RATE_LIMIT_SECRET=
